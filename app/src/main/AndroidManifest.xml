<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.odojadmin">

    <application
        android:name=".ApplicationMain"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activity.RegisterActivity"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity
            android:name=".view.activity.SignInActivity"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity android:name=".view.activity.RekapanActivity" />
        <activity
            android:name=".view.activity.SplashActivity"
            android:theme="@style/AppTheme.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.MainActivity"
            android:label="@string/title_activity_main"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.activity.AdminSettingActivity" />
        <activity android:name=".view.activity.CreateGroupActivity" />
        <activity
            android:name=".view.activity.DetailGroupActivity"
            android:theme="@style/AppTheme.NoTitleBar" />
        <activity android:name=".view.activity.GroupSettingActivity"
            android:theme="@style/AppTheme.NoTitleBar" ></activity>
    </application>

</manifest>